# Exploratory Data Analysis

This directory is used to experiment `eda/`.
This is especially useful for scripts developed using python first before refactored to julia.
Once an `eda/` scripts mature, it can be further promoted to `docs/`.
